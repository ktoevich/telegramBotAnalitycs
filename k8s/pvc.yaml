apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: telegram-bot-db
  namespace: shr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
